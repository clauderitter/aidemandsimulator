# Weekly model review — 2026-09-07

**Priority: high**

The central number this week is the growth_check gap (44% vs 18% per quarter, ~0.20 log/qtr, 4× the trigger threshold), and the rule meant to act on it cannot fire for another two weeks while pipe was moved 16→24 by instruction on the same evidence. Underneath that sits the mono identity, which makes the compute-constrained stance — the thing that decides whether the gap moves pipe or orgX — unfalsifiable, against two independent revenue-per-GW readings this week that imply q0 utilisation near 60%. Separately, evidence intake is failing on infrastructure rather than evidence: five feeds down 5/5 days, three proposals killed by 403s, six gauges stale, and one scenario-update path returning "scenario missing" for an active core scenario.

### structure: The derived rule mono = R0 ÷ (K0 × (1 − train)) has become a tautology: it forces q0 utilisation to 100%, so the standing compute-constrained stance can never be contradicted by data, and every revenue revision is absorbed as revenue per GW rather than as utilisation. This matters more than usual because the same stance decides whether the growth gap moves pipe or orgX.
- **Evidence:** R0 108.3 → 118.3 on 09-03 mechanically pushed mono 27.1 → 29.6 with no capacity or utilisation consequence. Two independent revenue-per-GW readings were logged and discarded in the researcher notes because 'mono is never moved by the pipeline': Patel/Dwarkesh 09-03 ('$15b a GW turned into $50b+ of revenue') and Tunguz 09-05 (Anthropic 'as high as $50m per megawatt', i.e. ~$50B/GW). At mono = 50, q0 utilisation is 118.3/(10 × 0.4 × 50) ≈ 59%.
- **Suggestion:** Human commit: invert the identity. Make mono a real parameter with a rule (revenue_per_gw: lease-rate and revenue-per-MW disclosures from labs/clouds, estimate-graded, ±10% per run, bounds ~20–60) and derive util0 = R0 ÷ (K0 × (1 − train) × mono) as a gauge. Add a re-baseline check that flags when util0 leaves 0.85–1.00, and rewrite the growth_calibration rule to read util0 rather than the stance text.
- **Expected effect:** q0 stops being rationed by assumption. If mono lands near the observed 40–50, scarcity pricing (scarce = 0.5) and the overhang branch switch off at t0, realised revenue = demand at t0, build commits fire later, and the 0.20 log/qtr growth gap is re-attributed from pipe to orgX — a materially different central path from the same inputs.

### rules: The growth_calibration gate is unsatisfiable in practice while the handle it guards is being moved around it. The three-consecutive-week condition has no stored gauge history, and the gauge only entered the changelog on 09-04, so the researcher declined to act on it four days running (09-04, 09-05, 09-06, 09-07) — while the collector moved pipe 16 → 22.4 → 24 (+50%) on 09-03/09-04 under the same rule name, citing the maintainer's instruction rather than the gauge condition.
- **Evidence:** gauge:growth_check has read '44% vs 18% per qtr' unchanged on 09-04 through 09-07 — a gap of ln1.44 − ln1.18 ≈ 0.20 log/qtr, four times the 0.05 threshold. Changelog: 'pipe 16 → 22.4 — Calibration on the maintainer's instruction'; 'buildMax 0.2 → 0.25 — Calibration'; orgX 1 → 1.1 rejected 09-07; four researcher notes state the three-week condition is unverified. pipe was also the only parameter to hit a speed limit this week.
- **Suggestion:** Store growth_check in state.json as one reading per ISO week plus a weeks_at_gap counter, so the clock actually runs and is auditable; require any collector-side calibration move to cite the same counter (no instruction-only path); and add a fast lane: when the gap exceeds 0.15 log/qtr for two weeks, one capped move toward closing half of it is allowed immediately.
- **Expected effect:** Either q0 growth rises from 18% toward the observed 44% via the handle the stance selects, pulling the demand/capacity crossing forward by roughly a year in central runs, or the gap is formally recorded as a rule failure instead of being silently absorbed by pipe. Also stops pipe and buildMax from carrying calibration that belongs to demand.

### inputs: H0 and D0 are the oldest live inputs (as-of 2026-04-07, five months) and nothing ages the horizon seed to quarter zero; meanwhile all this week's capability evidence arrives in metrics no rule can read, so H is anchored to a pre-reasoning-era measurement while the model has rolled forward two quarters.
- **Evidence:** Notes on 09-03, 09-04, 09-05, 09-06 and the 09-07 re-baseline all confirm METR's page last updated 2026-05-08 and no 80% horizon for GPT-6 Astra or Claude Fable 5.1. Rejected/unmappable this week: Epoch ECI trend break (6 → 14–15 pts/yr since reasoning models; Astra 169 vs Fable 163), UK AISI no-CoT horizon 30.9 min vs 3.6 min for Sol. H0 = 3.1 h was measured in 2026Q2 but is used as the 2026Q3 seed; at d ≈ 3/4.2 = 0.71 doublings/qtr, one quarter of ageing puts H near 5 h.
- **Suggestion:** Two small rules. (1) H0_asof: if the METR measurement quarter precedes q0, roll H0 forward by 2^d per elapsed quarter and grade it derived. (2) An estimate-graded bridge for D0 from Epoch's ECI doubling trend, usable only when the newest METR 80% reading is more than 120 days old, capped at ±0.3 months/run and auto-superseded the moment METR publishes.
- **Expected effect:** Restores movement to d, H, sat_oth (H_sat = 40) and rdBoostFactor, which are currently frozen on an April benchmark; expect a modestly faster horizon path and earlier oth-segment saturation, and it ends the four-note-per-week dead end where real capability news maps to no key.

### sources: The judge's re-fetch gate is rejecting evidence for infrastructure reasons, not evidentiary ones, and two watchlist entries added this week have never returned a byte.
- **Evidence:** Feed health: David Cahn, Zvi, Import AI, Econ Lab failed 5/5 days; Epoch Gradient Updates failed 4/4 days since being added on 09-03; Econ Lab was added 09-03 and has never succeeded. Substack feeds return empty 81-byte bodies (09-05 note). Three proposals died on 403 alone: scenario:middle (epochai.substack), scenario:oaiaccel and event:2026Q3 (openai.com). The 09-07 note admits some quotes came from search-engine renderings rather than direct fetches.
- **Suggestion:** Fix the fetcher for Substack (feed endpoint plus a browser user-agent) and openai.com; and amend judge rule 1 to accept a quote verified against a dated archive snapshot (web.archive.org) when the live page returns 403/empty, with the snapshot URL and capture date recorded in the changelog entry. If a watchlist source fails five consecutive runs, mark it dead in state.json rather than leaving it in the rotation.
- **Expected effect:** Recovers the gap-camp and Epoch revenue streams that currently reach the pipeline only second-hand, unblocks the middle scenario update and the 2026Q3 Astra event, and removes the current incentive to verify quotes from search snippets.

### data: The fetch/search budget was exhausted mid-session on every single run this week, and it is being spent before the housekeeping work, so six gauges have rotted while the weekly re-baseline is no longer independent of the incremental values.
- **Evidence:** 'Budget was exhausted mid-session' or equivalent on 09-03, 09-04, 09-05, 09-06 and 09-07. Stale: fed_fsr (2026-05-01), h100_rental (2026-04-02), token_index (2026-07-03), openrouter_cn (2026-07-09), capex_ocf (2026-06-16), vc_h1 (2026-07-02) — flagged as unrefreshable for budget reasons four days running. The 09-07 re-baseline of K0, pipe, train, capexGW and the sh_* shares reports 'no new sourced figure today' and simply finds itself within limits of the current values.
- **Suggestion:** Reorder the daily run: refresh the stale-gauge list and the re-baseline fetches first, opportunistic scanning last, with a per-run cap on scanning. Cache fetched pages for 7 days so the weekly re-baseline does not re-spend budget on the same sources. Auto-flag any gauge older than 60 days as unusable in the dashboards rather than displaying it as current.
- **Expected effect:** openrouter_cn and token_index feed the migration and price-collapse views (subst is at 20 on 09-03 evidence and h100_rental/capex_ocf back the financing and cost side, where fin just moved 1 → 1.3); refreshing them changes which of subst, fin and capexGW the next proposals can defend, and makes the re-baseline capable of surfacing a wrong rule instead of confirming the increment.

### scenarios: After the 09-03 rule change the 15% path test now mostly protects a cluster of near-identical core scenarios, so the admission gate has become a moratorium: two of four rotating slots sit empty while newcomers are rejected for resembling scenarios the pipeline is forbidden to retire. Camp coverage and proponent positions are otherwise fine.
- **Evidence:** Five core pairs are inside the gate already: base/supercycle 5%, rate/credit 6%, gap/wall 10%, enterprise/coding 11%, base/shock 12%. Rejections this week: scenario:epochplans 'not distinct from grid (paths within 12% at every anchor)'; scenario:debtwave folded into credit; the Tunguz Meta data-barter view folded into price by the researcher (09-04 note) to avoid duplicating a live view. Only nvfactory and rampconc occupy rotating slots, both added 09-03, no churn since.
- **Suggestion:** Human commit: run the distinctness test on the mechanism vector (parameter overrides plus shock types) as the primary criterion, and apply the path-similarity test only against active rotating scenarios — core scenarios are protected, so they should not also be blockers. Separately, look at base vs supercycle: a bull flagship 5% from base is a sign the overrides do not bite, probably because capacity binds in both.
- **Expected effect:** Fills 1–2 rotating slots (the Epoch largest-site slowdown and the debt-capacity view are the live candidates) and widens the fan at the 2028 anchor, which is currently narrower than the disagreement among the cited proponents.

### data: The scenario-update path fails for active core scenarios, leaving core scenario text pinned to superseded numbers.
- **Evidence:** 09-07: 'rejected scenario:contagion — Gate: scenario missing', for a scenario listed as active [bear, core]. The proposal carried Zitron's 06-09 revision of committed spend from $1.1T to $1.3T; the contagion thesis text therefore still reads $1.1T. Contrast scenario:price, which updated successfully on 09-04 — the difference looks like the updater only resolving ids it is allowed to retire.
- **Suggestion:** Fix the id lookup so scenario_update resolves core scenarios (retirement stays blocked, text and source updates do not). Add a regression check that every active scenario id is resolvable by the update path on each run.
- **Expected effect:** Keeps core scenario theses in step with their proponents' current positions, which is the only way the review can tell a stale scenario from a proponent who has genuinely changed view.

---
Filed automatically by the pipeline. Context: [changelog](https://github.com/clauderitter/aidemandsimulator/blob/main/site/data/changelog.json) · [live site](https://www.aidemandsimulator.com/). Inputs move on their own under the rulebook; anything here needs a commit.
