# Weekly model review — 2026-09-18

**Priority: high**

Three things need a human commit rather than another week of proposals: the calibration gap cannot be closed by orgX arithmetically, so the fast lane is now permanent and every token-intensity finding is being laundered into one global handle; the realised 16 Sep hike is recorded both as a lasting rate shock and in rateGap; and the util0 branch can no longer select the supply side, which makes the calibration rule single-valued. Separately, k_rd was cut by the new drift cap and restored to 0.8 the same day on the class of evidence the rulebook excludes, four core scenarios now share one revenue path, and the researcher has run out of web budget on all seven runs this week.

### structure: The model has no channel for tokens per task, so all evidence about rising per-task token spend is being pushed into orgX, where it cannot arithmetically close the calibration gap. The gap is not converging: 0.26 → 0.245 → 0.224 log/qtr over W36–W38, and today's collected reading widened to +56% observed vs +25% modelled.
- **Evidence:** Calibration ledger weeks_at_gap=3, fast_lane=true. orgX went 1.0 → 1.1 (8 Sep) → 1.2 (18 Sep); the 1.1→1.2 step is worth ln(2.2/2.1)/4 ≈ 0.012 log/qtr of quarter-zero drive. Closing half of a 0.22 gap needs ≈0.11 log/qtr, i.e. orgX ≈ 2.4, ten drift-capped steps away and applied to every segment including oth. The evidence behind the move is intensity, not adoption breadth: Brown's Ultra Mode customers paying 2x tokens for a 2x speedup, Tunguz's 14x agent token growth (1m vs 100–200m tokens/user/day), 130B tokens over 88 hours from 10,000 agents.
- **Suggestion:** Commit a per-task intensity term: multiply segment drive by an intensity factor tied to H (or to d) with its own input, e.g. tok_int, so 'same tasks, more tokens per task' has a home separate from organic user growth. Until that exists, change the growth_check rule so the demand-side branch routes to the segment drive the evidence names (rd/swe eps_i) rather than global orgX, and so a gap that survives six consecutive weeks escalates to a maintainer issue instead of generating another capped orgX step.
- **Expected effect:** Quarter-zero growth rises where the evidence points instead of uniformly; oth stops being inflated by AI-R&D intensity findings; the ledger stops reporting a permanent fast lane, and the weekly orgX ratchet (three proposals this week, two rejected as double-counts of the same Epoch write-up) stops.

### rules: The realised 16 Sep hike is being counted twice: as an accepted event:2026Q3 rate +0.25 lasting shock and, once DFF prints the new range, again through rateGap.
- **Evidence:** 'accepted event:2026Q3: → rate 0.25' (17 Sep, FOMC 12–0 to 3-3/4–4%). rateGap = −0.05 as of 16 Sep because fedfunds is still 3.63%; rate0 stepped 4.0 → 4.1 the same week. The rulebook already states that a realised hike 'shows up here [rateGap] and fades as the average catches up', while rate = rate0 + rateGap + shocks adds the event on top.
- **Suggestion:** Grade realised policy moves as events for the record only, with zero shock magnitude, and reserve shock_rate for moves not yet in DFF; add a judge check that rejects a rate shock whose quarter contains a realised FOMC change already flowing through rateGap. Retro-expire the 2026Q3 entry's shock leg.
- **Expected effect:** Removes a persistent ~+0.25pp overstatement of rate − rate0 in every scenario from 2026Q3 on, which currently depresses M via exp(−m_rate·(rate−rate0)) and the build-financing clamp (r_sens), understating commits and capacity through the whole horizon.

### rules: The new monthly drift cap on loop gains was applied and then undone within the same run, and the pending_target mechanism now carries the capped value forward, so the cap changes nothing.
- **Evidence:** 18 Sep: 'accepted k_rd: 0.8 → 0.7 — Reverted under the new monthly drift cap (±0.1 per 30 days)… raised twice in eleven days on evidence that labs use many agents, which measures the size of AI R&D spend' and, the same day, 'accepted k_rd: 0.7 → 0.8 — Anthropic's ~30,000 concurrent agents'. Speed-limit report: k_rd moved 0.7 → 0.8, target 0.8 carried over. The accepted rationale is the exact class the rulebook excludes ('evidence that usage is large is about shares, not gains'); the researcher's own note says the R&D Automation Index 'measures internal automation, not the revenue-token mix' and put it into k_rd anyway.
- **Suggestion:** Two gate changes: (a) a parameter reverted for breaching drift30 gets a 30-day cooldown against same-direction moves, and a capped move never stores pending_target when the binding cap is drift30 rather than the per-run cap; (b) require a loop-gain proposal to quote a numeric comparison of a segment's observed spend growth against the implied steady growth drive/(1−k) printed in the digest, and reject agent-count or concurrency figures by name.
- **Expected effect:** Stops the k_rd ping-pong (0.6→0.7→0.8→0.7→0.8 in a fortnight). At k_rd 0.8 versus 0.7 the rd steady multiplier is 5x versus 3.3x, so this single key is currently the largest unforced swing in the 2028 revenue path.

### rules: The util0 branch of the calibration rule has become single-valued: with the current inputs quarter zero can never be rationed, so 'pipe (then buildMax)' is unreachable and the rule is effectively 'always move orgX'.
- **Evidence:** util0 = 118.3 / (10 × 0.5 × 50) = 47.3%, flat at 47% on every collected reading since 11 Sep. Flipping the branch needs mono ≈ 95 or K0 ≈ 5 — both far outside the ±10%/run drift on mono and contradicted by the 17 Sep K0 rejection. The researcher has written the conclusion out twice ('util0 is 47% (slack), so pipe is not the calibration handle', 16 Sep), and the 18 Sep fidelity pass records that since mono became evidence-driven 'every scenario inherited the base case's slack'.
- **Suggestion:** Test the branch on the model's own quarter-zero state — whether latent demand exceeds capacity so scarcity pricing is active — or on an external scarcity signal (rental index, delivery lead times), not on util0. Print in the digest the mono/K0 values that would flip the branch so the choice is visible.
- **Expected effect:** Restores the supply-side calibration path: when the gap is genuinely rationing, pipe/buildMax move and the revenue path becomes capacity-limited rather than absorbing everything into demand drive. Also removes the false impression that a 47% gauge is an independent check on mono.

### inputs: The capability channel is frozen. H0 and D0 are 164 days old and METR has stopped publishing, so d = (3/D0)·… is pinned to the March 2026 state of the art while three capability releases have shipped since.
- **Evidence:** H0 = 5.08, D0 = 4.2, both as of 2026-04-07. 'METR time-horizons page still reads LAST UPDATED May 8, 2026' (16, 17, 18 Sep notes). Meanwhile GPT-6 Astra (ECI 166, SWE-ECI 164) and FrontierMath Tier 4 going 5% → 98% in 14 months were all logged as 'capability news' with no rule to map them.
- **Suggestion:** Either allow a second source of truth for the horizon rule (a dated third-party 80%-success horizon measurement, graded estimate, with a wider bound) or add an explicit staleness policy: past 120 days without a METR print, flag H0/D0 as stale on the site and widen the d uncertainty band rather than treating a five-month-old reading as current.
- **Expected effect:** Unfreezes eps_i·epsX·d, the one channel that should be carrying capability-driven demand; today that drive is understated, which is part of what the orgX ratchet is compensating for, and H's trajectory (hence sat_oth and ceilingSlow) is anchored to a stale level.

### scenarios: Four core scenarios now produce one path. base, supercycle, physics and shock are all within 14% at every anchor, with supercycle vs physics at 3% and base vs physics at 6% — the collapse dates from the 18 Sep fidelity pass.
- **Evidence:** Path-similarity table (base/supercycle 7%, base/physics 6%, supercycle/physics 3%, shock/physics 14%); 'accepted scenarios: relative → faithful — Scenario fidelity pass' the same day. Likely cause: derived overrides of the form '=util0:0.95' now resolve to the same effective mono (≈25 at current inputs) in several scenarios, so a bull capex view and a physics-constrained view land on the same supply curve.
- **Suggestion:** Human commit: audit which core scenarios use '=util0:0.95' and re-fit the two that are meant to differ in mechanism (supercycle should differ through buildMax/lead/capexGW, physics through lead and buildMax with base mono). If they still coincide after re-fitting, merge or retire one — the pipeline cannot, since both are core.
- **Expected effect:** The displayed fan re-opens; the 15% distinctness gate regains meaning for newcomers (it currently only screens against rotating scenarios, and all four rotating slots are full after swarm was added on 18 Sep, so the next admissible view will retire rampconc or elastic).

### scenarios: Two scenarios have drifted from their proponents. grid rests on a leg its own author has retracted, and the pipeline is forbidden from re-fitting a core scenario's overrides; rampconc's proponent may have switched camp.
- **Evidence:** 16 Sep: 'scenario:grid: updated — the scenario's own proponent has revised the local-opposition leg it rested on — a parcel-level audit of 300+ moratoriums finds only ~2.3 GW genuinely delayed while the model still shows +38 GW of US IT capacity in 2027'. Under the lifecycle rule a core scenario_update 'may carry only new thesis text', so the overrides still encode the retracted constraint. rampconc is filed [bear, rotating] while the 17 Sep note records Kharazian's 10 Sep 'base case is bullish' thread, unread because Substack/x.com fetches failed.
- **Suggestion:** Commit re-fitted grid overrides (loosen the buildMax/lead constraint to the ~2.3 GW audited delay, keep the behind-the-meter leg) or retire it with the reason recorded. Separately, read the Kharazian thread through a working channel and either re-file rampconc to the camp its proponent now holds or retire it and free a rotating slot.
- **Expected effect:** Removes a structural bear path the source no longer supports (grid currently drags the low end of the capacity fan) and stops the bear camp being counted one scenario stronger than its live proponents.

### sources: Five watchlist feeds have failed on every run for at least seven consecutive runs and are still being retried daily instead of being marked dead, and they are precisely the sources behind two scenarios and one collector.
- **Evidence:** Feed health 11–18 Sep, identical line each day: 'failed David Cahn, Don't Worry About the Vase (Zvi), Import AI (Jack Clark), Econ Lab (Ara Kharazian, Ramp Economics Lab), Epoch AI — Gradient Updates'. Cahn is the gap scenario's proponent, Kharazian rampconc's; Epoch Gradient Updates is the revenue channel. The five-failure dead-marking and Monday-only retry rule is evidently not firing.
- **Suggestion:** Fix the endpoints (Substack feeds resolve at <publication>/feed, not the JS homepage; Epoch's newsletter has a separate feed from epoch.ai/latest, which was added 17 Sep) and enforce the dead-marking so failures leave the daily digest. Where no feed exists, record the source as fetch-only for the researcher.
- **Expected effect:** Restores the inputs that the two proponent checks in the item above depend on, and cuts a recurring daily false alarm that currently hides real feed breakage.

### data: The researcher exhausted its web_search/web_fetch budget on all seven runs this week, producing zero gauge proposals, and it spends that budget re-checking gauges whose next publication date is known. The usage ledger the rulebook requires is also empty.
- **Evidence:** 'TOOL LIMIT: web_search and web_fetch quotas were exhausted mid-session' (16 Sep) and equivalents on 11, 12, 15, 17, 18 Sep. fed_fsr was fetched on six runs with the note 'next edition is not due until ~Nov 2026'; vc_h1 on five with 'Q3 2026 does not close until 2026-09-30'; capex_ocf fetched daily and verified unchanged each time. Casualties of the exhausted budget: METR verification, openrouter.ai/rankings, the Epoch AI Chip Users Explorer (the best candidate for a train/K0 move), the bonds YTD tally. Cost report reads 'no ledger yet' despite the rule that every run appends to pipeline/state/usage.json.
- **Suggestion:** Enforce cadence_days before spending a fetch (fed_fsr semiannual, vc_h1 quarterly, capex_ocf on its own insight-page date) and apply the 3–14 day backoff on a miss; give housekeeping a separate budget from evidence work so a failed early search batch cannot starve the METR and capacity checks. Fix the usage.json append, which is silently not running.
- **Expected effect:** Frees roughly a third of the weekly fetch budget for the inputs that actually move outputs (H0/D0, train, K0), so stale-gauge notes stop crowding the digest; and restores cost visibility, which is currently zero.

---
Filed automatically by the pipeline. Context: [changelog](https://github.com/clauderitter/aidemandsimulator/blob/main/site/data/changelog.json) · [live site](https://www.aidemandsimulator.com/). Inputs move on their own under the rulebook; anything here needs a commit.
