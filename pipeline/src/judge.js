// Judge agent: verifies proposals against sources and rules, applies the accepted ones. Implemented in the next commit.
export async function judge(state, proposals, limits, changelog) { return { accepted: 0, rejected: 0 }; }
