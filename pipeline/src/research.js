// Researcher agent: sweeps the watchlist and emits proposals. Never edits state. Implemented in the next commit.
export async function research(state, cfg, work) { return { proposals: [], notes: ['researcher not yet enabled'] }; }
